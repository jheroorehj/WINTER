# ── 데이터셋 (공개 저장소에 올리지 않음) ────────────────────────────
# 원본 데이터셋과 전처리 산출물. 용량이 크고 재배포 조건 확인이 끝나지 않았다.
archive/

# 화면 확인용 샘플 8장은 frontend/project/sample/ 에 커밋한다(재배포 조건은 미확정 —
# frontend/project/sample/README.md 참고). 원본 데이터셋만 제외한다.

# ── OS / 편집기 ──────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# ── 모델 산출물 (추후 학습 결과물이 들어올 자리) ──────────────────
*.h5
*.ckpt
*.pth
*.tfrecord
