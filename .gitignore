# ── 데이터셋 (공개 저장소에 올리지 않음) ────────────────────────────
# 원본 데이터셋과 전처리 산출물. 용량이 크고 재배포 조건 확인이 끝나지 않았다.
archive/

# 프로토타입 화면 확인용으로 데이터셋에서 복사해 둔 안저 샘플.
# 로컬에서만 두고 쓴다 — 복원 방법은 README 의 "데모 이미지" 항목 참고.
frontend/project/uploads/fundus/

# ── OS / 편집기 ──────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# ── 모델 산출물 (추후 학습 결과물이 들어올 자리) ──────────────────
*.h5
*.ckpt
*.pth
*.tfrecord
