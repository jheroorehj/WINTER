# ── 데이터셋 (공개 저장소에 올리지 않음) ────────────────────────────
# 원본 데이터셋과 전처리 산출물. 용량이 크고 재배포 조건 확인이 끝나지 않았다.
# 시연 시나리오에 쓰는 14장만 data/images/ 로 뽑아 커밋한다.
archive/
test_dataset_seed42/

# 화면 확인용 샘플 8장(frontend/project/sample/)과 시연 시나리오 14장(data/images/)은
# 커밋한다. 재배포 조건은 미확정 — README 의 데이터셋 항목 참고.

# ── OS / 편집기 ──────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# ── 모델 산출물 (추후 학습 결과물이 들어올 자리) ──────────────────
*.h5
*.ckpt
*.pth
*.tfrecord
